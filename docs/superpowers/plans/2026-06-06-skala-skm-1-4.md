# Penyesuaian Skala Penilaian ke 1–4 (PermenPAN-RB 14/2017) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mengubah skala penilaian survei dari 1–5 (bintang) menjadi 1–4 (skala persepsi resmi PermenPAN-RB 14/2017), dengan IKM = rata-rata(1–4) × 25 tanpa konversi.

**Architecture:** Backend CI4 (validasi 1–4, helper IKM tanpa konversi). Frontend React (komponen tombol berlabel `RatingScale` menggantikan `StarRating`, konstanta bersama `NILAI_PERSEPSI` & `UNSUR_LABEL`, dashboard berbasis skala 4). Data: fresh start (tabel `survei` dikosongkan).

**Tech Stack:** CodeIgniter 4 + PHPUnit; React 19 + TypeScript + Vitest + Testing Library.

**Spec:** `docs/superpowers/specs/2026-06-06-skala-skm-1-4-design.md`

---

## File Structure

**Backend (modify):**
- `app/Helpers/ikm_helper.php` — hapus `rata5_ke_nrr`; `hitung_ikm` = rata-rata(1–4) × 25.
- `app/Controllers/Api/SurveiController.php` — validasi `less_than[5]`.
- `app/Models/SurveiModel.php` — komentar IKM disesuaikan.
- `tests/unit/IkmHelperTest.php`, `tests/models/SurveiModelTest.php`, `tests/controllers/api/SurveiControllerTest.php`.

**Frontend (create):**
- `frontend/src/components/survey/RatingScale.tsx` + `RatingScale.test.tsx`.

**Frontend (modify):**
- `frontend/src/lib/ikm.ts` — hapus `rata5ToNrr`; `hitungIkm` = rata-rata × 25; tambah `NILAI_PERSEPSI` & `UNSUR_LABEL`.
- `frontend/src/pages/SurveyPage.tsx` — pakai `RatingScale` + `UNSUR_LABEL`.
- `frontend/src/components/dashboard/SummaryCards.tsx` — `/5`→`/4`, label dari `UNSUR_LABEL`.
- `frontend/src/components/dashboard/PetugasDetailDialog.tsx` — `/5`→`/4`, ambang `low`, label.
- `frontend/src/components/dashboard/RatingDistribution.tsx` — buckets 4, teks "1–4", label.
- `frontend/src/pages/DashboardPage.tsx` — ambang `lowSaran` + teks.

**Frontend (delete):**
- `frontend/src/components/survey/StarRating.tsx` + `StarRating.test.tsx`.

**Frontend (update tests):**
- `frontend/src/pages/SurveyPage.test.tsx`, `frontend/src/pages/DashboardPage.test.tsx`, `frontend/src/components/dashboard/AnomaliPanel.test.tsx` (jika berasumsi skala 5).

**Data (operasional):** kosongkan tabel `survei`.

---

## FASE A — BACKEND

### Task A1: Helper IKM tanpa konversi skala

**Files:**
- Modify: `app/Helpers/ikm_helper.php`
- Test: `tests/unit/IkmHelperTest.php`

- [ ] **Step 1: Tulis ulang test helper (RED)**

Ganti seluruh isi `tests/unit/IkmHelperTest.php`:

```php
<?php

namespace Tests\Unit;

use CodeIgniter\Test\CIUnitTestCase;

/**
 * Unit test helper IKM (PermenPAN-RB 14/2017).
 * Skala penilaian 1..4 = NRR langsung; IKM = rata-rata unsur x 25.
 *
 * @internal
 */
final class IkmHelperTest extends CIUnitTestCase
{
    protected function setUp(): void
    {
        parent::setUp();
        helper('ikm');
    }

    public function testHitungIkmMengikutiRumusPermenPANRB(): void
    {
        // Semua "Sangat Baik" (4) -> IKM 100
        $this->assertSame(100.0, hitung_ikm(4.0, 4.0, 4.0, 4.0));
        // Semua "Baik" (3) -> IKM 75 (mutu C, ambang B mulai NRR ~3,06)
        $this->assertSame(75.0, hitung_ikm(3.0, 3.0, 3.0, 3.0));
        // Semua "Kurang Baik" (2) -> IKM 50
        $this->assertSame(50.0, hitung_ikm(2.0, 2.0, 2.0, 2.0));
        // Semua "Tidak Baik" (1) -> IKM 25 (batas bawah)
        $this->assertSame(25.0, hitung_ikm(1.0, 1.0, 1.0, 1.0));
    }

    public function testHitungIkmMerataRatakanUnsurYangTidakSeragam(): void
    {
        // Rata-rata (1+2+3+4)/4 = 2.5 -> IKM 62.5
        $this->assertSame(62.5, hitung_ikm(1.0, 2.0, 3.0, 4.0));
    }

    public function testHitungIkmNolUntukSeluruhUnsurKosong(): void
    {
        $this->assertSame(0.0, hitung_ikm(0.0, 0.0, 0.0, 0.0));
    }
}
```

- [ ] **Step 2: Jalankan test, verifikasi gagal**

Run: `vendor/bin/phpunit tests/unit/IkmHelperTest.php --no-coverage`
Expected: FAIL (`hitung_ikm(4,4,4,4)` masih menghasilkan 100 via konversi lama? cek) — khususnya `(3,3,3,3)` lama = `(((3-1)/4)*3+1)*25` = 62.5, baru harus 75 → FAIL. `rata5_ke_nrr` masih ada.

- [ ] **Step 3: Tulis ulang helper (GREEN)**

Ganti seluruh isi `app/Helpers/ikm_helper.php`:

```php
<?php

/**
 * Helper kalkulasi IKM sesuai PermenPAN-RB 14/2017.
 *
 * Skala penilaian aplikasi adalah 1..4 (sama dengan NRR PermenPAN), sehingga
 * Nilai IKM dihitung langsung: rata-rata seluruh unsur (skala 1..4) dikali 25,
 * menghasilkan skala 25..100. Tidak ada konversi skala. Helper ini menjadi
 * sumber kebenaran tunggal di backend dan mencerminkan frontend/src/lib/ikm.ts.
 */

if (! function_exists('hitung_ikm')) {
    /**
     * Menghitung Nilai IKM (25..100) dari rata-rata empat unsur survei
     * pada skala 1..4, sesuai PermenPAN-RB 14/2017.
     *
     * @param float $kecepatan  Rata-rata unsur kecepatan (1..4)
     * @param float $keramahan  Rata-rata unsur keramahan (1..4)
     * @param float $informasi  Rata-rata unsur informasi (1..4)
     * @param float $kenyamanan Rata-rata unsur kenyamanan (1..4)
     * @return float Nilai IKM 25..100 (atau 0 jika seluruh unsur kosong)
     */
    function hitung_ikm(float $kecepatan, float $keramahan, float $informasi, float $kenyamanan): float
    {
        $unsur = [$kecepatan, $keramahan, $informasi, $kenyamanan];

        return round((array_sum($unsur) / count($unsur)) * 25, 2);
    }
}
```

- [ ] **Step 4: Jalankan test, verifikasi lulus**

Run: `vendor/bin/phpunit tests/unit/IkmHelperTest.php --no-coverage`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add app/Helpers/ikm_helper.php tests/unit/IkmHelperTest.php
git commit -m "refactor(ikm): hitung IKM langsung dari skala 1-4 (tanpa konversi)"
```

---

### Task A2: Validasi rating 1–4 di controller

**Files:**
- Modify: `app/Controllers/Api/SurveiController.php:28-31`
- Test: `tests/controllers/api/SurveiControllerTest.php`

- [ ] **Step 1: Perbarui test controller (RED)**

Di `tests/controllers/api/SurveiControllerTest.php`, ubah `testSubmitSuksesDenganRatingValid` (nilai 5 → 4 karena 5 kini invalid):

```php
    public function testSubmitSuksesDenganRatingValid(): void
    {
        $result = $this->withBodyFormat('json')->call('post', '/api/survei', [
            'petugas_id' => 1,
            'kecepatan'  => 4,
            'keramahan'  => 3,
            'informasi'  => 4,
            'kenyamanan' => 3,
            'saran'      => 'Mantap',
        ]);

        $result->assertStatus(201);

        $count = $this->db->table('survei')->countAllResults();
        $this->assertSame(1, $count);
    }
```

Ganti `testSubmitGagalDenganRatingDiluar1Sampai5` menjadi (nilai 5 kini ditolak):

```php
    public function testSubmitGagalDenganRatingDiluar1Sampai4(): void
    {
        $result = $this->withBodyFormat('json')->call('post', '/api/survei', [
            'petugas_id' => 1,
            'kecepatan'  => 5,
            'keramahan'  => 3,
            'informasi'  => 4,
            'kenyamanan' => 3,
        ]);

        $result->assertStatus(422);
    }
```

Ubah `testSubmitGagalUntukPetugasNonAktif` (nilai 5 → 4 agar lolos validasi, gagal hanya karena petugas non-aktif):

```php
        $result = $this->withBodyFormat('json')->call('post', '/api/survei', [
            'petugas_id' => 1,
            'kecepatan'  => 4,
            'keramahan'  => 4,
            'informasi'  => 4,
            'kenyamanan' => 4,
        ]);
```

- [ ] **Step 2: Jalankan test, verifikasi gagal**

Run: `vendor/bin/phpunit tests/controllers/api/SurveiControllerTest.php --no-coverage`
Expected: FAIL pada `testSubmitGagalDenganRatingDiluar1Sampai4` (nilai 5 masih diterima 201, validasi lama `less_than[6]`).

- [ ] **Step 3: Perbarui validasi (GREEN)**

Di `app/Controllers/Api/SurveiController.php`, ubah baris 28-31 dari `less_than[6]` menjadi `less_than[5]`:

```php
            'kecepatan'  => 'required|integer|greater_than[0]|less_than[5]',
            'keramahan'  => 'required|integer|greater_than[0]|less_than[5]',
            'informasi'  => 'required|integer|greater_than[0]|less_than[5]',
            'kenyamanan' => 'required|integer|greater_than[0]|less_than[5]',
```

- [ ] **Step 4: Jalankan test, verifikasi lulus**

Run: `vendor/bin/phpunit tests/controllers/api/SurveiControllerTest.php --no-coverage`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add app/Controllers/Api/SurveiController.php tests/controllers/api/SurveiControllerTest.php
git commit -m "feat(survei): batasi rating ke skala 1-4 pada validasi backend"
```

---

### Task A3: Sesuaikan test & komentar SurveiModel ke skala 1–4

**Files:**
- Modify: `app/Models/SurveiModel.php` (komentar IKM)
- Test: `tests/models/SurveiModelTest.php`

- [ ] **Step 1: Tulis ulang test model ke skala 1–4 (RED)**

Ganti dua metode test berikut di `tests/models/SurveiModelTest.php`:

```php
    public function testGetRekapByDateRangeMenghitungIKMDenganBenar(): void
    {
        $model = new SurveiModel();

        // 2 survei petugas 1, semua "Sangat Baik" (4) -> IKM 100
        $model->insert(['petugas_id' => 1, 'kecepatan' => 4, 'keramahan' => 4, 'informasi' => 4, 'kenyamanan' => 4, 'saran' => null]);
        $model->insert(['petugas_id' => 1, 'kecepatan' => 4, 'keramahan' => 4, 'informasi' => 4, 'kenyamanan' => 4, 'saran' => null]);

        $today = date('Y-m-d');
        $rekap = $model->getRekapByDateRange($today, $today);

        $this->assertSame(2, $rekap['summary']['total_responden']);
        $this->assertSame(100.0, (float) $rekap['summary']['ikm']);
        $this->assertCount(1, $rekap['per_petugas']);
        $this->assertSame('Budi Santoso', $rekap['per_petugas'][0]['nama']);
        $this->assertSame(2, $rekap['per_petugas'][0]['total_responden']);
    }

    public function testGetRekapByDateRangeMenghitungRataRataPerPetugas(): void
    {
        $model = new SurveiModel();

        // Petugas 1: semua "Baik" (3); Petugas 2: semua "Sangat Baik" (4)
        $model->insert(['petugas_id' => 1, 'kecepatan' => 3, 'keramahan' => 3, 'informasi' => 3, 'kenyamanan' => 3]);
        $model->insert(['petugas_id' => 2, 'kecepatan' => 4, 'keramahan' => 4, 'informasi' => 4, 'kenyamanan' => 4]);

        $today = date('Y-m-d');
        $rekap = $model->getRekapByDateRange($today, $today);

        $this->assertSame(2, $rekap['summary']['total_responden']);
        // Rata-rata semua unsur = (3+4)/2 = 3.5 -> IKM 3.5 * 25 = 87.5
        $this->assertSame(87.5, (float) $rekap['summary']['ikm']);
    }
```

Ganti metode batas bawah:

```php
    public function testGetRekapByDateRangeMenghitungIKMSesuaiPermenPANRB(): void
    {
        $model = new SurveiModel();

        // Seluruh unsur "Tidak Baik" (1) -> IKM 1 * 25 = 25 (batas bawah)
        $model->insert(['petugas_id' => 1, 'kecepatan' => 1, 'keramahan' => 1, 'informasi' => 1, 'kenyamanan' => 1]);

        $today = date('Y-m-d');
        $rekap = $model->getRekapByDateRange($today, $today);

        $this->assertSame(1, $rekap['summary']['total_responden']);
        $this->assertSame(25.0, (float) $rekap['summary']['ikm']);
    }
```

- [ ] **Step 2: Jalankan test, verifikasi gagal**

Run: `vendor/bin/phpunit tests/models/SurveiModelTest.php --no-coverage`
Expected: FAIL pada `testGetRekapByDateRangeMenghitungRataRataPerPetugas` (rata 3.5: lama via konversi = `(((3.5-1)/4)*3+1)*25` = 71.875→71.88, baru = 87.5).

- [ ] **Step 3: Sesuaikan komentar SurveiModel (GREEN)**

Di `app/Models/SurveiModel.php`, ganti blok komentar di atas pemanggilan `hitung_ikm` menjadi:

```php
        // IKM dihitung sesuai PermenPAN-RB 14/2017 (skala 25..100) lewat helper
        // (di-autoload via Config\Autoload::$helpers). Skala penilaian 1..4 =
        // NRR langsung, jadi IKM = rata-rata unsur * 25 tanpa konversi.
        $ikm = hitung_ikm(
```

(Logika `hitung_ikm` sudah benar dari Task A1; tidak ada perubahan kode lain.)

- [ ] **Step 4: Jalankan test, verifikasi lulus**

Run: `vendor/bin/phpunit tests/models/SurveiModelTest.php --no-coverage`
Expected: PASS (4 tests).

- [ ] **Step 5: Jalankan seluruh test backend (regresi)**

Run: `vendor/bin/phpunit --no-coverage`
Expected: PASS (semua hijau).

- [ ] **Step 6: Commit**

```bash
git add app/Models/SurveiModel.php tests/models/SurveiModelTest.php
git commit -m "test(survei): sesuaikan ekspektasi IKM ke skala 1-4"
```

---

## FASE B — FRONTEND (logika & komponen)

> Semua perintah frontend dijalankan dari direktori `frontend/`.

### Task B1: lib/ikm.ts — rumus tanpa konversi + konstanta bersama

**Files:**
- Modify: `frontend/src/lib/ikm.ts`
- Test: `frontend/src/lib/ikm.test.ts` (Create)

- [ ] **Step 1: Tulis test lib/ikm (RED)**

Create `frontend/src/lib/ikm.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { hitungIkm, categorizeIkm, NILAI_PERSEPSI, UNSUR_LABEL } from './ikm'

describe('hitungIkm (skala 1-4)', () => {
  it('semua unsur 4 -> IKM 100', () => {
    expect(hitungIkm({ kecepatan: 4, keramahan: 4, informasi: 4, kenyamanan: 4 })).toBe(100)
  })
  it('semua unsur 3 -> IKM 75', () => {
    expect(hitungIkm({ kecepatan: 3, keramahan: 3, informasi: 3, kenyamanan: 3 })).toBe(75)
  })
  it('semua unsur 1 -> IKM 25', () => {
    expect(hitungIkm({ kecepatan: 1, keramahan: 1, informasi: 1, kenyamanan: 1 })).toBe(25)
  })
  it('unsur tidak seragam (1,2,3,4) -> IKM 62.5', () => {
    expect(hitungIkm({ kecepatan: 1, keramahan: 2, informasi: 3, kenyamanan: 4 })).toBe(62.5)
  })
})

describe('konstanta SKM', () => {
  it('NILAI_PERSEPSI berisi 4 label resmi', () => {
    expect(NILAI_PERSEPSI.map((p) => p.label)).toEqual([
      'Tidak Baik',
      'Kurang Baik',
      'Baik',
      'Sangat Baik',
    ])
  })
  it('UNSUR_LABEL memetakan kunci ke label tampilan', () => {
    expect(UNSUR_LABEL.keramahan).toBe('Keramahan & Perilaku')
  })
})

describe('categorizeIkm (tidak berubah)', () => {
  it('100 -> A', () => expect(categorizeIkm(100).grade).toBe('A'))
  it('75 -> C', () => expect(categorizeIkm(75).grade).toBe('C'))
})
```

- [ ] **Step 2: Jalankan test, verifikasi gagal**

Run: `npx vitest run src/lib/ikm.test.ts`
Expected: FAIL (`NILAI_PERSEPSI`/`UNSUR_LABEL` belum diekspor; `hitungIkm({3..})` lama = 75? cek: lama `rata5ToNrr(3)=2.5 ->*25=62.5` → FAIL).

- [ ] **Step 3: Perbarui lib/ikm.ts (GREEN)**

Di `frontend/src/lib/ikm.ts`: (a) **hapus** fungsi `rata5ToNrr`; (b) ganti `hitungIkm`; (c) tambah konstanta. Ganti blok dari baris `/** Map nilai bintang... */` sampai akhir file menjadi:

```ts
/** Label nilai persepsi resmi PermenPAN-RB 14/2017 (skala 1..4). */
export const NILAI_PERSEPSI = [
  { nilai: 1, label: 'Tidak Baik' },
  { nilai: 2, label: 'Kurang Baik' },
  { nilai: 3, label: 'Baik' },
  { nilai: 4, label: 'Sangat Baik' },
] as const

/** Label tampilan 4 unsur (kolom DB tetap). */
export const UNSUR_LABEL: Record<
  'kecepatan' | 'keramahan' | 'informasi' | 'kenyamanan',
  string
> = {
  kecepatan: 'Kecepatan Pelayanan',
  keramahan: 'Keramahan & Perilaku',
  informasi: 'Kejelasan Informasi',
  kenyamanan: 'Kenyamanan',
}

/** Hitung IKM dari rata-rata 4 unsur (skala 1..4). IKM = rata-rata x 25. */
export function hitungIkm(aspek: {
  kecepatan: number
  keramahan: number
  informasi: number
  kenyamanan: number
}): number {
  const nilai = [aspek.kecepatan, aspek.keramahan, aspek.informasi, aspek.kenyamanan]
  const rataRata = nilai.reduce((sum, v) => sum + v, 0) / nilai.length
  return Number((rataRata * 25).toFixed(2))
}
```

Juga perbarui komentar header file (baris 1-16) agar tidak menyebut skala 1..5/konversi: ganti deskripsi menjadi "Skala penilaian aplikasi: 1..4 (= NRR PermenPAN). Nilai IKM = rata-rata unsur x 25 (skala 25..100)."

- [ ] **Step 4: Jalankan test, verifikasi lulus**

Run: `npx vitest run src/lib/ikm.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/lib/ikm.ts frontend/src/lib/ikm.test.ts
git commit -m "refactor(ikm): hitungIkm skala 1-4 + konstanta NILAI_PERSEPSI & UNSUR_LABEL"
```

---

### Task B2: Komponen RatingScale (ganti StarRating)

**Files:**
- Create: `frontend/src/components/survey/RatingScale.tsx`
- Create: `frontend/src/components/survey/RatingScale.test.tsx`

- [ ] **Step 1: Tulis test RatingScale (RED)**

Create `frontend/src/components/survey/RatingScale.test.tsx`:

```tsx
import { fireEvent, render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it, vi } from 'vitest'
import { RatingScale } from './RatingScale'

describe('RatingScale', () => {
  it('merender 4 opsi persepsi sebagai radiogroup', () => {
    render(<RatingScale value={0} onChange={() => {}} label="Test" />)
    expect(screen.getByRole('radiogroup', { name: 'Penilaian Test' })).toBeInTheDocument()
    expect(screen.getAllByRole('radio')).toHaveLength(4)
  })

  it('menampilkan label persepsi resmi', () => {
    render(<RatingScale value={0} onChange={() => {}} label="Test" />)
    expect(screen.getByRole('radio', { name: /tidak baik \(nilai 1\)/i })).toBeInTheDocument()
    expect(screen.getByRole('radio', { name: /sangat baik \(nilai 4\)/i })).toBeInTheDocument()
  })

  it('memanggil onChange dengan nilai saat diklik', () => {
    const onChange = vi.fn()
    render(<RatingScale value={0} onChange={onChange} label="Test" />)
    fireEvent.click(screen.getByRole('radio', { name: /sangat baik \(nilai 4\)/i }))
    expect(onChange).toHaveBeenCalledWith(4)
  })

  it('menandai opsi aktif sesuai value', () => {
    render(<RatingScale value={2} onChange={() => {}} label="Test" />)
    const opsi = screen.getByRole('radio', { name: /kurang baik \(nilai 2\)/i })
    expect(opsi).toHaveAttribute('aria-checked', 'true')
    expect(opsi).toHaveAttribute('data-active', 'true')
  })

  it('mendukung navigasi keyboard panah', async () => {
    const onChange = vi.fn()
    const user = userEvent.setup()
    render(<RatingScale value={2} onChange={onChange} label="Test" />)
    screen.getByRole('radio', { name: /kurang baik \(nilai 2\)/i }).focus()
    await user.keyboard('{ArrowRight}')
    await user.keyboard('{ArrowLeft}')
    expect(onChange).toHaveBeenNthCalledWith(1, 3)
    expect(onChange).toHaveBeenNthCalledWith(2, 2)
  })
})
```

- [ ] **Step 2: Jalankan test, verifikasi gagal**

Run: `npx vitest run src/components/survey/RatingScale.test.tsx`
Expected: FAIL (modul `./RatingScale` belum ada).

- [ ] **Step 3: Buat RatingScale.tsx (GREEN)**

Create `frontend/src/components/survey/RatingScale.tsx`:

```tsx
import { useRef } from 'react'
import { NILAI_PERSEPSI } from '@/lib/ikm'
import { cn } from '@/lib/utils'

interface RatingScaleProps {
  value: number
  onChange: (value: number) => void
  label: string
}

// Penilaian 4-poin berlabel persepsi resmi PermenPAN-RB 14/2017.
// Aksesibel sebagai radiogroup dengan navigasi panah keyboard.
export function RatingScale({ value, onChange, label }: RatingScaleProps) {
  const groupRef = useRef<HTMLDivElement | null>(null)

  const fokuskanNilai = (next: number) => {
    onChange(next)
    groupRef.current
      ?.querySelector<HTMLButtonElement>(`[data-nilai="${next}"]`)
      ?.focus()
  }

  return (
    <div className="flex flex-col gap-2">
      <span className="text-sm font-medium text-foreground">{label}</span>
      <div
        ref={groupRef}
        className="grid grid-cols-2 gap-2 sm:grid-cols-4"
        role="radiogroup"
        aria-label={`Penilaian ${label}`}
      >
        {NILAI_PERSEPSI.map(({ nilai, label: persepsi }) => {
          const active = value === nilai
          return (
            <button
              key={nilai}
              type="button"
              role="radio"
              aria-checked={active}
              aria-label={`${persepsi} (nilai ${nilai})`}
              data-nilai={nilai}
              data-active={active}
              tabIndex={value === 0 ? (nilai === 1 ? 0 : -1) : active ? 0 : -1}
              onClick={() => onChange(nilai)}
              onKeyDown={(event) => {
                if (event.key === 'ArrowRight' || event.key === 'ArrowDown') {
                  event.preventDefault()
                  fokuskanNilai(Math.min(4, nilai + 1))
                }
                if (event.key === 'ArrowLeft' || event.key === 'ArrowUp') {
                  event.preventDefault()
                  fokuskanNilai(Math.max(1, nilai - 1))
                }
              }}
              className={cn(
                'flex flex-col items-center gap-1 rounded-xl border px-2 py-3 text-center transition-colors',
                'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary',
                active
                  ? 'border-blue-500 bg-blue-500/10 text-blue-700 dark:text-blue-300'
                  : 'border-border hover:border-blue-400 hover:bg-muted',
              )}
            >
              <span className="text-lg font-bold tabular-nums">{nilai}</span>
              <span className="text-xs font-medium leading-tight">{persepsi}</span>
            </button>
          )
        })}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Jalankan test, verifikasi lulus**

Run: `npx vitest run src/components/survey/RatingScale.test.tsx`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/survey/RatingScale.tsx frontend/src/components/survey/RatingScale.test.tsx
git commit -m "feat(survey): komponen RatingScale 4-poin berlabel persepsi resmi"
```

---

### Task B3: SurveyPage pakai RatingScale + label unsur

**Files:**
- Modify: `frontend/src/pages/SurveyPage.tsx`
- Test: `frontend/src/pages/SurveyPage.test.tsx`

- [ ] **Step 1: Periksa & perbarui SurveyPage.test.tsx (RED)**

Buka `frontend/src/pages/SurveyPage.test.tsx`. Ganti setiap interaksi yang memilih "bintang" menjadi memilih opsi persepsi. Pola umum: ganti pemilihan `getAllByRole('radio')` (5 bintang) menjadi memilih 4 opsi, dan untuk melengkapi survei pilih satu opsi per aspek. Contoh helper pengisian yang harus dipakai (sesuaikan dengan struktur test yang ada):

```tsx
// Lengkapi 4 aspek dengan memilih opsi "Sangat Baik (nilai 4)" pada tiap radiogroup
const groups = screen.getAllByRole('radiogroup')
groups.forEach((g) => {
  const opsi = within(g).getByRole('radio', { name: /sangat baik \(nilai 4\)/i })
  fireEvent.click(opsi)
})
```

(Pastikan `import { within } from '@testing-library/react'`.) Hapus asersi yang spesifik bintang (mis. mengandung teks "Sangat Puas"/jumlah 5 bintang).

- [ ] **Step 2: Jalankan test, verifikasi gagal**

Run: `npx vitest run src/pages/SurveyPage.test.tsx`
Expected: FAIL (SurveyPage masih memakai `StarRating`; radiogroup berisi 5 bintang, label persepsi belum ada).

- [ ] **Step 3: Perbarui SurveyPage.tsx (GREEN)**

Di `frontend/src/pages/SurveyPage.tsx`:
- Ganti import: hapus `import { StarRating } from '@/components/survey/StarRating'`, tambah `import { RatingScale } from '@/components/survey/RatingScale'` dan `import { UNSUR_LABEL } from '@/lib/ikm'`.
- Ganti definisi `ASPEK` (baris 22-27) menjadi memakai label dari `UNSUR_LABEL`:

```tsx
const ASPEK: Array<{ key: keyof Ratings; label: string }> = [
  { key: 'kecepatan', label: UNSUR_LABEL.kecepatan },
  { key: 'keramahan', label: UNSUR_LABEL.keramahan },
  { key: 'informasi', label: UNSUR_LABEL.informasi },
  { key: 'kenyamanan', label: UNSUR_LABEL.kenyamanan },
]
```

- Ganti penggunaan komponen (baris ~207-214) dari `<StarRating ... />` menjadi:

```tsx
                      {ASPEK.map(({ key, label }) => (
                        <RatingScale
                          key={key}
                          label={label}
                          value={ratings[key]}
                          onChange={(v) => setRatings((prev) => ({ ...prev, [key]: v }))}
                        />
                      ))}
```

- [ ] **Step 4: Jalankan test, verifikasi lulus**

Run: `npx vitest run src/pages/SurveyPage.test.tsx`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/pages/SurveyPage.tsx frontend/src/pages/SurveyPage.test.tsx
git commit -m "feat(survey): SurveyPage memakai RatingScale 1-4 + label unsur baru"
```

---

### Task B4: Hapus StarRating

**Files:**
- Delete: `frontend/src/components/survey/StarRating.tsx`, `frontend/src/components/survey/StarRating.test.tsx`

- [ ] **Step 1: Pastikan tidak ada importer tersisa**

Run: `grep -rn "StarRating" src` (dari `frontend/`)
Expected: tidak ada hasil (semua importer sudah diganti di B3).

- [ ] **Step 2: Hapus file**

```bash
git rm frontend/src/components/survey/StarRating.tsx frontend/src/components/survey/StarRating.test.tsx
```

- [ ] **Step 3: Jalankan seluruh test frontend (regresi)**

Run: `npx vitest run` (dari `frontend/`)
Expected: PASS (tidak ada referensi StarRating yang hilang).

- [ ] **Step 4: Commit**

```bash
git commit -m "chore(survey): hapus komponen StarRating (digantikan RatingScale)"
```

---

## FASE C — FRONTEND (dashboard)

### Task C1: SummaryCards berbasis skala 4

**Files:**
- Modify: `frontend/src/components/dashboard/SummaryCards.tsx`

- [ ] **Step 1: Perbarui pembagi & label (tanpa test khusus — diverifikasi via DashboardPage.test)**

Di `SummaryCards.tsx`:
- Tambah import: `import { categorizeIkm, UNSUR_LABEL } from '@/lib/ikm'` (gabung dengan import `categorizeIkm` yang ada).
- Pada `ASPEK_META`, ganti tiap `label` menjadi nilai dari `UNSUR_LABEL` (mis. `label: UNSUR_LABEL.kecepatan`, dst) untuk keempat unsur.
- Baris 172: ganti `/ 5.00` menjadi `/ 4.00`.
- Baris 178: ganti `(summary.rata_rata[key] / 5) * 100` menjadi `(summary.rata_rata[key] / 4) * 100`.

- [ ] **Step 2: Jalankan test dashboard (regresi awal)**

Run: `npx vitest run src/pages/DashboardPage.test.tsx`
Expected: bisa FAIL bila test lama mengasumsi `/ 5.00` atau label lama — akan diperbaiki di Task C4.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/dashboard/SummaryCards.tsx
git commit -m "feat(dashboard): SummaryCards berbasis skala 4 + label unsur baru"
```

---

### Task C2: PetugasDetailDialog berbasis skala 4

**Files:**
- Modify: `frontend/src/components/dashboard/PetugasDetailDialog.tsx`

- [ ] **Step 1: Perbarui pembagi, ambang, label**

Di `PetugasDetailDialog.tsx`:
- Tambahkan `UNSUR_LABEL` ke import dari `@/lib/ikm`.
- Pada `ASPEK` (baris 36-41), ganti tiap `label` menjadi `UNSUR_LABEL[key]` yang sesuai (mis. `{ key: 'kecepatan', label: UNSUR_LABEL.kecepatan, color: 'var(--chart-1)' }`).
- Baris 118: ganti `{v.toFixed(2)} / 5` menjadi `{v.toFixed(2)} / 4`.
- Baris 123: ganti `${(v / 5) * 100}%` menjadi `${(v / 4) * 100}%`.
- Baris 145: ganti ambang `const low = avg < 4` menjadi `const low = avg < 3` (rata-rata di bawah "Baik" pada skala 1–4).

- [ ] **Step 2: Jalankan test (regresi)**

Run: `npx vitest run src/pages/DashboardPage.test.tsx`
Expected: lanjut ke Task C4 untuk perbaikan test.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/dashboard/PetugasDetailDialog.tsx
git commit -m "feat(dashboard): PetugasDetailDialog berbasis skala 4"
```

---

### Task C3: RatingDistribution 4 nilai berlabel persepsi

**Files:**
- Modify: `frontend/src/components/dashboard/RatingDistribution.tsx`

- [ ] **Step 1: Perbarui buckets, loop, label**

Di `RatingDistribution.tsx`:
- Tambah import: `import { NILAI_PERSEPSI, UNSUR_LABEL } from '@/lib/ikm'`.
- Pada `ASPEK` (baris 11-40), ganti tiap `label` menjadi `UNSUR_LABEL[key]` yang sesuai.
- Baris 45: ganti `const buckets = [0, 0, 0, 0, 0]` menjadi `const buckets = [0, 0, 0, 0]`.
- Baris 48: ganti `if (v >= 1 && v <= 5)` menjadi `if (v >= 1 && v <= 4)`.
- Baris 65: ganti teks `memberi 1–5 bintang` menjadi `memberi nilai 1–4 (Tidak Baik s/d Sangat Baik)`.
- Pada render bucket (baris 89-132): pertahankan struktur, tetapi tampilkan label persepsi. Ganti penanda `<Star ...>{star}` menjadi nomor + label dari `NILAI_PERSEPSI`. Minimal: ganti `const isLow = star <= 2` tetap valid (1,2 = rendah pada skala 4); dan pada `aria-label` & teks gunakan `NILAI_PERSEPSI[i].label`. Contoh penyesuaian penanda kiri:

```tsx
                        <span className="flex items-center gap-1 tabular-nums text-muted-foreground">
                          {star}. {NILAI_PERSEPSI[i].label}
                        </span>
```

dan `aria-label`:

```tsx
                          aria-label={`${label} — ${NILAI_PERSEPSI[i].label}: ${count} respon (${pct.toFixed(1)}%)`}
```

(Impor `Star` boleh dihapus bila tidak lagi dipakai.)

- [ ] **Step 2: Jalankan test (regresi)**

Run: `npx vitest run src/pages/DashboardPage.test.tsx`
Expected: lanjut Task C4.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/dashboard/RatingDistribution.tsx
git commit -m "feat(dashboard): distribusi rating 1-4 berlabel persepsi"
```

---

### Task C4: DashboardPage — ambang saran rendah + sinkron test

**Files:**
- Modify: `frontend/src/pages/DashboardPage.tsx`
- Test: `frontend/src/pages/DashboardPage.test.tsx`

- [ ] **Step 1: Perbarui ambang & teks di DashboardPage.tsx**

Di `frontend/src/pages/DashboardPage.tsx`:
- Baris ~465-468: ganti ambang `return avg < 3` menjadi `return avg < 2.5` (di bawah titik tengah skala 1–4).
- Baris ~477: ganti teks `rating sangat rendah (&lt;3 bintang)` menjadi `rating rendah (rata-rata &lt; 2,5 dari 4)`.

- [ ] **Step 2: Sinkronkan DashboardPage.test.tsx (RED→GREEN)**

Buka `frontend/src/pages/DashboardPage.test.tsx`. Ubah data mock & asersi yang berbasis skala 5:
- Nilai unsur mock yang `5` → `4`; sesuaikan asersi IKM yang diharapkan memakai rumus baru (rata-rata × 25).
- Ganti asersi teks `/ 5.00` (jika ada) → `/ 4.00`; teks "1–5 bintang" → "1–4".
- Sesuaikan label unsur bila di-assert (mis. "Keramahan" → "Keramahan & Perilaku").

Run: `npx vitest run src/pages/DashboardPage.test.tsx`
Expected: PASS setelah penyesuaian.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/DashboardPage.tsx frontend/src/pages/DashboardPage.test.tsx
git commit -m "feat(dashboard): ambang saran rendah utk skala 4 + sinkron test"
```

---

### Task C5: Sapu bersih sisa asumsi skala 5 + regresi penuh

**Files:**
- Modify (jika perlu): `frontend/src/components/dashboard/AnomaliPanel.test.tsx` dan file lain yang masih berasumsi skala 5.

- [ ] **Step 1: Cari sisa referensi skala 5**

Run (dari `frontend/`): `grep -rn -E "rata5|/ 5\b|<= 5|1–5|1-5| 5 bintang|bintang" src`
Expected: hanya sisa yang benar (mis. tidak ada). Perbaiki setiap sisa yang keliru ke basis skala 4. Untuk `AnomaliPanel.test.tsx`, periksa apakah `5` yang muncul adalah nilai unsur (ubah ke 1–4) atau angka lain (mis. jumlah/tanggal — biarkan).

- [ ] **Step 2: Jalankan SELURUH test frontend**

Run: `npx vitest run` (dari `frontend/`)
Expected: PASS semua.

- [ ] **Step 3: Build produksi (verifikasi kompilasi & deploy artefak)**

Run: `npm run build` (dari `frontend/`)
Expected: build sukses; `public/app` terupdate.

- [ ] **Step 4: Commit (bila ada perubahan)**

```bash
git add -A
git commit -m "test(frontend): bersihkan sisa asumsi skala 5"
```

---

## FASE D — DATA (operasional, fresh start)

### Task D1: Kosongkan data uji survei

**Files:** tidak ada perubahan kode.

> **Catatan timing:** langkah ini adalah **langkah rilis/deploy**, dijalankan saat menerapkan perubahan ke lingkungan yang melayani aplikasi (mis. port 8082) — BUKAN di tengah pengembangan branch (DB bersifat shared, bukan per-branch). CI4 4.7.2 tidak memiliki command `db:query`.

- [ ] **Step 1: Kosongkan tabel survei** (mempertahankan petugas & admin), via MySQL client:

```bash
mysql -u admin_baru -p survey_petugas_db -e "TRUNCATE TABLE survei;"
```

Atau reset penuh (drop + migrate ulang + reseed petugas/admin — menghapus juga data petugas uji):

```bash
php spark migrate:refresh && php spark db:seed App\\Database\\Seeds\\DatabaseSeeder
```

- [ ] **Step 2: Verifikasi tabel survei kosong**

```bash
mysql -u admin_baru -p survey_petugas_db -e "SELECT COUNT(*) AS n FROM survei;"
```
Expected: `n = 0`.

> **Penting (data lama skala 5):** jika ada data survei lama bernilai 5 yang tidak dikosongkan, dashboard akan menghitung IKM dari data campur (mis. `hitung_ikm(5,5,5,5)=125`, melebihi 100). Karena itu fresh start WAJIB sebelum aplikasi skala-4 dipakai dengan data nyata.

---

## Verifikasi Akhir (Acceptance)

- [ ] Backend: `vendor/bin/phpunit --no-coverage` hijau.
- [ ] Frontend: `npx vitest run` hijau (dari `frontend/`).
- [ ] `grep -rn "rata5" app/ frontend/src` → kosong (tidak ada konversi tersisa).
- [ ] `grep -rn "StarRating" frontend/src` → kosong.
- [ ] Form survei menampilkan 4 tombol berlabel (Tidak Baik/Kurang Baik/Baik/Sangat Baik).
- [ ] Backend menolak nilai 5 (422), menerima 1–4.
- [ ] Dashboard konsisten skala 4 (kartu `/4.00`, distribusi 1–4, IKM = rata-rata × 25).
- [ ] Tabel `survei` kosong dari data uji.
